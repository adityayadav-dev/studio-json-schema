import MonacoEditor from "../components/MonacoEditor";
// import BottomBar from "../components/BottomBar"; // Kept commented out as per original App.tsx

const EditorPage = () => {
    return (
        <>
            <MonacoEditor />
            {/* <BottomBar /> */}
        </>
    );
};

export default EditorPage;
